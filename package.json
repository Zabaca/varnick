{
  "name": "varnick",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --filter @varnick/core dev",
    "build": "bun run --filter @varnick/core build",
    "drive": "bun run --filter @varnick/core drive",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test packages"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@types/bun": "latest",
    "typescript": "7"
  }
}
